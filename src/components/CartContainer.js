import React from "react";
import styled from "styled-components";

const CartContainer = () => {
  return <ContainerCart>/Cart</ContainerCart>;
};

const ContainerCart = styled.div``;

export default CartContainer;
